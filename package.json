{
  "name": "everyrow-sheets",
  "version": "0.1.0",
  "description": "Google Sheets add-on for everyrow operations (rank, dedupe, screen)",
  "private": true,
  "scripts": {
    "setup": "node scripts/setup.js",
    "push": "clasp push",
    "push:watch": "clasp push --watch",
    "pull": "clasp pull",
    "open": "clasp open",
    "logs": "clasp logs",
    "logs:watch": "clasp logs --watch",
    "deploy": "clasp deploy",
    "versions": "clasp versions"
  },
  "devDependencies": {
    "@google/clasp": "^2.4.2"
  },
  "engines": {
    "node": ">=18"
  }
}
