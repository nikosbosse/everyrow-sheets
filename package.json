{
  "name": "everyrow-sheets",
  "version": "0.1.0",
  "description": "Google Sheets add-on for everyrow operations (rank, screen, dedupe, merge, agent)",
  "private": true,
  "scripts": {
    "clasp:init": "node scripts/setup.js",
    "test:api": "node scripts/test-api.js",
    "push": "clasp push --force",
    "push:watch": "clasp push --watch",
    "pull": "clasp pull",
    "open": "clasp open",
    "logs": "clasp logs",
    "logs:watch": "clasp logs --watch",
    "deploy": "clasp deploy",
    "versions": "clasp versions",
    "deploy:push": "node scripts/deploy.js push",
    "deploy:version": "node scripts/deploy.js version",
    "deploy:status": "node scripts/deploy.js status",
    "deploy:help": "node scripts/deploy.js help"
  },
  "devDependencies": {
    "@google/clasp": "^2.4.2"
  },
  "engines": {
    "node": ">=18"
  }
}
